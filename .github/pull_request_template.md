## What this does

## Why

## Tests
Which tests cover this? (`cd backend && python -m pytest tests/ -q` — CI
runs the same. New behavior needs a new test; see tests/ for house style.)

## Provenance
If this PR adds anything the graph asserts, where does its evidence come
from?
